import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Evaluate — Greenlit',
}

export default function EvaluateLayout({ children }: { children: React.ReactNode }) {
  return children
}
